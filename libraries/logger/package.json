{
  "name": "logger",
  "version": "0.0.2",
  "description": "Logging utility to be commonly used by other components",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "lint": "eslint --parser tsconfig.json",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Adam Staniszewski",
  "license": "MIT",
  "dependencies": {
    "pino": "^8.5.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.0",
    "typescript": "^4.8.3"
  }
}
